[project]
name = "maia-outreach-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "anthropic",
  "python-telegram-bot==21.0.1",
  "requests",
  "python-dotenv",
  "upstash-redis",
]

[tool.vercel]
entrypoint = "api.webhook:handler"
